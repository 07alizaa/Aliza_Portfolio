services:
  frontend:
    build: ./frontend
    container_name: aliza_frontend
    ports:
      - "3000:3000"
    environment:
      - HOST=0.0.0.0
    command: vite preview --host 0.0.0.0 --port 3000
    restart: unless-stopped
